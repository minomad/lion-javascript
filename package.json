{
  "scripts": {
    "start":"live-server client --host=127.0.0.1 --port=5500 --no-browser",
    "serve": "node server/index.js"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "live-server": "^1.2.2",
    "prettier": "^2.8.8"
  }
}
